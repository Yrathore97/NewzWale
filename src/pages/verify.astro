---
// D8 (NEWZWALE_IMPLEMENTATION_PLAN.md): /verify → /fact-check, permanent 301.
//
// A PERMANENT redirect, not a replacement, because /verify is in
// public/sitemap.xml (removed by this same change), linked from the footer,
// FactCheckPromo, and the floating CTA, and is presumably indexed
// (NEWZWALE_ARCHITECTURE.md §4.1). Rollback is exactly this: revert this file
// and the page renders again.
return Astro.redirect('/fact-check', 301);
---
